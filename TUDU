make files


Variablen:
  invoice=Rechnung
  offer=Angebot


commands:
  ./ascii -n, --new "name"
    * create subfolder "name"
    * cd to subfolder
    * add Makefile
    * open yml in VIM
  ./ascii -i, --invoice "name"
    * create tex from first .yml
    * pdflatex .yml
    * cp new pdf to ./$NO-$NAME-rechnung.pdf
  ./ascii -o, --offer "name"
    * almost same as invoice
    * cp new pdf to ./$NO-$NAME-angebot.pdf
  ./ascii "name"
    * open project "name"
    * if not existend ask to do "./ascii -n"
